<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\User\Domain\Entity\User" table="user" repository-class="App\User\Infrastructure\Repository\UserRepository">
        <id name="id" type="integer">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="email" type="string" length="180">
            <options>
                <option name="notnull">true</option>
            </options>
        </field>

        <field name="roles" type="json">
            <options>
                <option name="notnull">true</option>
            </options>
        </field>

        <field name="password" type="string">
            <options>
                <option name="notnull">true</option>
            </options>
        </field>

        <one-to-many field="tasks" target-entity="App\Task\Domain\Entity\Task" mapped-by="owner">
            <cascade>
                <cascade-persist/>
            </cascade>
        </one-to-many>

        <one-to-many field="projects" target-entity="App\Entity\Project" mapped-by="owner">
            <cascade>
                <cascade-persist/>
            </cascade>
        </one-to-many>

        <one-to-many field="comments" target-entity="App\Entity\Comment" mapped-by="author">
            <cascade>
                <cascade-persist/>
                <cascade-remove/>
            </cascade>
        </one-to-many>

        <unique-constraints>
            <unique-constraint name="UNIQ_IDENTIFIER_EMAIL" columns="email"/>
        </unique-constraints>
    </entity>

</doctrine-mapping>
